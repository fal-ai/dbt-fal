import org.junit.Test

class TestFalFlow {

  @Test
  fun runFlow() {
    //    val client = TemporalConfig().workflowClient("****", "****")
    //    val projectId = "***"
    //    runDbtFlow(client, projectId)
  }
}
