name: "fal_test"
version: "1.0.0"
config-version: 2
profile: "fal_test"
source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "mockTarget"
clean-targets:
  - "target"
  - "dbt_modules"
