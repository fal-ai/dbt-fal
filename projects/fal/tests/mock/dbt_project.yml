name: "fal_test"
version: "1.0.0"
config-version: 2
profile: "fal_test"
model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
# TODO (target-path): Deprecated but keeping until https://github.com/dbt-labs/dbt-core/issues/7465 is resolved
target-path: "mockTarget"
clean-targets:
  - "target"
  - "dbt_modules"
